on:
    pull_request_target:
     branches: [ main ]
     types: [opened]
    pull_request:
     branches: [ main ]
     types: [opened]   
    issue_comment:
      types: [created]   
  
  jobs:
    check_pr_comments:
      runs-on: ubuntu-latest
      name: Check PR comments contain strings
      steps:
        - name: Check PR comments contain string
          id: String-comment-check
          uses: amine-arb-2019/test-actions-2@v1.1
          with:
            label-name-to-watch-for: '@graalvmbot, signed'  
            
          
          
  
